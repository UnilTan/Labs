<Window x:Class="MilkProductsBinding.Lab15StylesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ЛР-15: Демонстрация стилей и шаблонов" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource WinStyle}">
    
    <Window.Resources>
        <!-- Дополнительные стили для демонстрации -->
        
        <!-- Стиль для круглой кнопки -->
        <Style x:Key="CircleButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="LightBlue" Stroke="DarkBlue" StrokeThickness="2"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для градиентной кнопки -->
        <Style x:Key="GradientButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" BorderThickness="2" BorderBrush="DarkGreen">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="LightGreen" Offset="0"/>
                                    <GradientStop Color="DarkGreen" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                            TextElement.Foreground="White" TextElement.FontWeight="Bold"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Анимированный стиль кнопки -->
        <Style x:Key="AnimatedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Orange"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                              To="Red" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                              To="Orange" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="ЛР-15: Демонстрация стилей и шаблонов WPF" 
                   FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" 
                   Margin="10" Foreground="White"/>
        
        <!-- Основное содержимое -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <StackPanel>
                
                <!-- Секция 1: Базовые стили -->
                <GroupBox Header="1. Базовые стили из App.xaml" Margin="5">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Эти стили применяются автоматически ко всем элементам:" 
                                   FontWeight="Bold" Margin="5" Foreground="White"/>
                        
                        <WrapPanel Margin="10">
                            <Button Content="Обычная кнопка" Margin="5"/>
                            <Button Content="Еще кнопка" Margin="5"/>
                            <Button Content="Акцентная кнопка" Style="{StaticResource AccentButtonStyle}" Margin="5"/>
                        </WrapPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <TextBlock Text="TextBox:" VerticalAlignment="Center" Margin="5" Foreground="White"/>
                            <TextBox Text="Стилизованный TextBox" Width="200" Margin="5"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                
                <!-- Секция 2: Шаблоны кнопок -->
                <GroupBox Header="2. Пользовательские шаблоны кнопок" Margin="5">
                    <WrapPanel Margin="10">
                        <StackPanel Margin="10">
                            <TextBlock Text="Круглая кнопка:" Foreground="White" Margin="2"/>
                            <Button Content="●" Width="60" Height="60" Style="{StaticResource CircleButtonStyle}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="10">
                            <TextBlock Text="Градиентная кнопка:" Foreground="White" Margin="2"/>
                            <Button Content="Градиент" Width="100" Height="40" Style="{StaticResource GradientButtonStyle}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="10">
                            <TextBlock Text="Анимированная кнопка:" Foreground="White" Margin="2"/>
                            <Button Content="Наведи мышь!" Width="120" Height="40" Style="{StaticResource AnimatedButtonStyle}"/>
                        </StackPanel>
                    </WrapPanel>
                </GroupBox>
                
                <!-- Секция 3: Canvas с фоном -->
                <GroupBox Header="3. Canvas с фоновым изображением" Margin="5">
                    <Canvas Height="200" Width="300" Margin="10">
                        <Canvas.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="LightBlue" Offset="0"/>
                                <GradientStop Color="DarkBlue" Offset="1"/>
                            </LinearGradientBrush>
                        </Canvas.Background>
                        
                        <!-- Элементы на Canvas -->
                        <Button Content="Кнопка 1" Canvas.Left="20" Canvas.Top="20" 
                                Background="Red" Foreground="White" Width="80"/>
                        <Button Content="Кнопка 2" Canvas.Left="120" Canvas.Top="80" 
                                Background="Green" Foreground="White" Width="80"/>
                        <Button Content="Кнопка 3" Canvas.Left="200" Canvas.Top="140" 
                                Background="Orange" Foreground="White" Width="80"/>
                        
                        <!-- Декоративные элементы -->
                        <Ellipse Fill="Yellow" Width="30" Height="30" Canvas.Left="50" Canvas.Top="120"/>
                        <Rectangle Fill="Purple" Width="40" Height="25" Canvas.Left="150" Canvas.Top="30"/>
                    </Canvas>
                </GroupBox>
                
                <!-- Секция 4: DataGrid с стилями -->
                <GroupBox Header="4. DataGrid со стилизованными строками" Margin="5">
                    <DataGrid x:Name="dgDemo" Height="150" Margin="10" AutoGenerateColumns="True"/>
                </GroupBox>
                
                <!-- Секция 5: Различные элементы управления -->
                <GroupBox Header="5. Другие стилизованные элементы" Margin="5">
                    <StackPanel Margin="10">
                        <CheckBox Content="Стилизованный CheckBox" Margin="5" Foreground="White"/>
                        <RadioButton Content="Стилизованный RadioButton" Margin="5" Foreground="White"/>
                        <ComboBox x:Name="cmbDemo" Width="200" Margin="5" HorizontalAlignment="Left"/>
                        <Slider Minimum="0" Maximum="100" Value="50" Width="200" Margin="5" HorizontalAlignment="Left"/>
                        <ProgressBar Value="75" Width="200" Height="20" Margin="5" HorizontalAlignment="Left"/>
                    </StackPanel>
                </GroupBox>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="🔄 Обновить данные" Click="RefreshData_Click" Margin="5"/>
            <Button Content="🎨 Сменить тему" Click="ChangeTheme_Click" Margin="5"/>
            <Button Content="❌ Закрыть" Click="Close_Click" Style="{StaticResource AccentButtonStyle}" Margin="5"/>
        </StackPanel>
        
    </Grid>
</Window>