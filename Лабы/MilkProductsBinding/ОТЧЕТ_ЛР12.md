# Лабораторная работа 12 - DataGrid, ComboBox-столбцы, ListView

## Тема: Привязки данных Binding. Работа с БД через DataGrid и ListView

### Цель: изучить работу с DataGrid (редактируемый), ComboBox-столбцами и ListView для каталога товаров

---

## 🎯 ВЫПОЛНЕННЫЕ ЗАДАНИЯ

### ✅ 1. Работа с DataGrid (редактируемый)
**Что реализовано:**
- DataGrid с 3 текстовыми столбцами, привязанными к полям таблицы Product
- Указан способ изменения `Mode=TwoWay` для редактируемых полей
- Источник данных установлен через `ItemsSource = db.Product.ToList()`
- Кнопка сохранения изменений в базе данных

**XAML код:**
```xaml
<DataGrid x:Name="dtProduct" 
          AutoGenerateColumns="False" 
          CanUserAddRows="True" 
          CanUserDeleteRows="True">
    <DataGrid.Columns>
        <DataGridTextColumn Header="ID" 
                            Binding="{Binding idProduct}" 
                            IsReadOnly="True"/>
        <DataGridTextColumn Header="Наименование" 
                            Binding="{Binding nameProduct, Mode=TwoWay}"/>
        <DataGridTextColumn Header="Цена" 
                            Binding="{Binding priceProduct, Mode=TwoWay}"/>
        <DataGridTextColumn Header="Категория" 
                            Binding="{Binding Category, Mode=TwoWay}"/>
    </DataGrid.Columns>
</DataGrid>
```

**C# код:**
```csharp
// Установка источника данных
dtProduct.ItemsSource = db.Product.ToList();

// Сохранение изменений
private void SaveProductChanges_Click(object sender, RoutedEventArgs e)
{
    int changesCount = db.SaveChanges();
    MessageBox.Show($"Сохранено {changesCount} изменений!");
}
```

### ✅ 2. Работа с DataGrid и ComboBox-столбцом
**Что реализовано:**
- DataGrid с текстовыми столбцами, привязанными к полям таблицы DetailSale
- ComboBox-столбец для выбора продукта с привязкой к FK
- `SelectedValueBinding="{Binding IdProductDetailSale}"` - значение FK привязано к ComboBox
- Источник для ComboBox из таблицы Product

**XAML код:**
```xaml
<DataGrid x:Name="dtDetailSale">
    <DataGrid.Columns>
        <DataGridTextColumn Header="ID Детали" 
                            Binding="{Binding IdDetailSale}" 
                            IsReadOnly="True"/>
        <DataGridTextColumn Header="ID Продажи" 
                            Binding="{Binding IdSale, Mode=TwoWay}"/>
        
        <!-- ComboBox-столбец для выбора продукта -->
        <DataGridComboBoxColumn Header="Продукт" 
                                x:Name="cmbIdPrSale"
                                DisplayMemberPath="nameProduct"
                                SelectedValuePath="idProduct"
                                SelectedValueBinding="{Binding IdProductDetailSale, Mode=TwoWay}"/>
        
        <DataGridTextColumn Header="Количество" 
                            Binding="{Binding QuantityProduct, Mode=TwoWay}"/>
    </DataGrid.Columns>
</DataGrid>
```

**C# код:**
```csharp
// Источник для ComboBox-столбца
cmbIdPrSale.ItemsSource = db.Product.ToList();

// Источник для DataGrid
dtDetailSale.ItemsSource = db.DetailSale.Include(d => d.Product).ToList();

// Сохранение изменений
private void SaveDetailSaleChanges_Click(object sender, RoutedEventArgs e)
{
    int changesCount = db.SaveChanges();
    // Перезагружаем данные с обновленными связями
    dtDetailSale.ItemsSource = db.DetailSale.Include(d => d.Product).ToList();
}
```

### ✅ 3. Самостоятельные задания

#### А) ListView с привязкой к таблице
**Что реализовано:**
- ListView с привязкой к таблице Product
- Простая привязка через `ItemsSource`

```csharp
lvProducts.ItemsSource = db.Product.ToList();
```

#### Б) Каталог товаров в виде плиток (карточек)
**Что реализовано:**
- ListView с кастомным шаблоном отображения
- Каждый товар отображается как карточка с рамкой
- Показывается: название, цена, категория, срок годности, описание
- Фильтрация по категориям

**XAML код шаблона карточки:**
```xaml
<ListView x:Name="lvProducts">
    <ListView.ItemsPanel>
        <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
    </ListView.ItemsPanel>
    
    <ListView.ItemTemplate>
        <DataTemplate>
            <Border Background="LightYellow" 
                    BorderBrush="DarkBlue" 
                    BorderThickness="2" 
                    CornerRadius="10" 
                    Width="200" Height="150">
                <StackPanel>
                    <TextBlock Text="{Binding nameProduct}" 
                               FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding priceProduct, StringFormat='Цена: {0:C}'}" 
                               Foreground="DarkGreen"/>
                    <TextBlock Text="{Binding Category, StringFormat='Категория: {0}'}" 
                               Foreground="DarkBlue"/>
                    <TextBlock Text="{Binding ExpiryDays, StringFormat='Срок: {0} дней'}" 
                               Foreground="DarkRed"/>
                    <TextBlock Text="{Binding Description}" 
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>
```

**Фильтрация по категориям:**
```csharp
private void CategoryFilter_SelectionChanged(object sender, SelectionChangedEventArgs e)
{
    string selectedCategory = cmbCategoryFilter.SelectedItem.ToString();
    
    if (selectedCategory == "Все категории")
    {
        lvProducts.ItemsSource = db.Product.ToList();
    }
    else
    {
        var filteredProducts = db.Product.Where(p => p.Category == selectedCategory).ToList();
        lvProducts.ItemsSource = filteredProducts;
    }
}
```

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Используемые технологии:
- **WPF** (.NET 6.0)
- **Entity Framework Core** 6.0.25
- **SQL Server Express**
- **Data Binding** (простые и сложные привязки)
- **Mode=TwoWay** для двусторонней привязки

### Структура окна ЛР-12:
1. **Верхняя часть** - два DataGrid рядом:
   - Слева: редактируемый DataGrid с продуктами
   - Справа: DataGrid с деталями продаж и ComboBox-столбцом
2. **Нижняя часть** - ListView с каталогом товаров в виде карточек
3. **Управление** - кнопки загрузки, сохранения, фильтрации

### Ключевые особенности:
- **Автоматическое обновление** ComboBox при изменении продуктов
- **Фильтрация** ListView по категориям
- **Валидация** при сохранении данных
- **Информативные сообщения** о результатах операций

---

## 📸 ЧТО ПОКАЗАТЬ В ОТЧЕТЕ

### Скриншоты для отчета:
1. **Главное окно** с кнопкой "Открыть ЛР-12"
2. **Окно ЛР-12** - общий вид с тремя секциями
3. **DataGrid с продуктами** - до и после редактирования
4. **DataGrid с ComboBox-столбцом** - выбор продукта из списка
5. **ListView с карточками** товаров
6. **Фильтрация** по категориям в ListView
7. **Сообщения** о сохранении изменений

### Код для отчета:
1. **XAML** - структура DataGrid с ComboBox-столбцом
2. **XAML** - шаблон карточки для ListView
3. **C#** - привязка данных к DataGrid
4. **C#** - обработчик сохранения изменений
5. **C#** - фильтрация ListView

### Пояснения в отчете:
- Объяснить разницу между простой привязкой DataGrid и ComboBox-столбцом
- Показать, как работает `SelectedValueBinding` с FK
- Описать преимущества ListView с кастомными шаблонами
- Объяснить принцип работы фильтрации

---

## ✅ КРИТЕРИИ УСПЕШНОЙ СДАЧИ

### Преподаватель проверит:
1. **DataGrid редактируется** и сохраняет изменения в БД
2. **ComboBox-столбец** корректно работает с FK
3. **ListView** отображает товары в виде красивых карточек
4. **Фильтрация** работает без ошибок
5. **Код** структурирован и прокомментирован
6. **Интерфейс** интуитивно понятен

### Типичные ошибки (избегать!):
- Не указан `Mode=TwoWay` для редактируемых полей
- Не установлен `ItemsSource` для ComboBox-столбца
- Забыли `Include(d => d.Product)` при загрузке связанных данных
- Не обновляется ListView после изменений в продуктах
- Отсутствует обработка ошибок при сохранении

---

## 🚀 ЗАПУСК И ТЕСТИРОВАНИЕ

1. Запустите приложение
2. Нажмите "🚀 Открыть ЛР-12" в главном окне
3. Нажмите "Загрузить данные"
4. Протестируйте:
   - Редактирование в DataGrid продуктов
   - Выбор продукта в ComboBox-столбце
   - Сохранение изменений
   - Фильтрацию в ListView
   - Добавление тестового продукта

**Результат:** Полностью функциональное приложение, демонстрирующее все требования ЛР-12!